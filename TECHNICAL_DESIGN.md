# 桌面记事本技术方案

> 文档状态：v1.2 DeepSeek Flash 默认 Provider
> 产品代号：FeedNote（暂定）
> 核心定位：本地优先、可追溯、可演进的个人记忆系统
> 适用范围：MVP 至 v1 的产品、架构、安全与 AI 行为约束

## 1. 文档目的

本方案定义桌面记事本的产品边界、技术架构、数据模型、AI 权限和首版交付范围。

系统的目标不是替用户“写好一切”，而是降低记录成本，并把零散输入逐步整理成可检索、可关联、可追溯的个人记忆。

核心流程：

```text
投喂 Feed -> 理解 Understand -> 演进 Evolve -> 召回 Recall
```

传统笔记以“文档”为中心；本产品以“记忆状态”为中心。但记忆状态只能在保留原始证据和历史版本的前提下演进。

## 2. 产品原则

### 2.1 本地优先

- 原始输入、索引、模型输出和操作历史保存在本机；用户已明确授权把当前输入及最小候选上下文发送给配置的 DeepSeek 模型。
- 核心记录、检索和浏览能力在断网状态下必须可用。
- 云模型承担当前自动理解能力；模型不可用时，记录、检索和浏览仍须正常工作。

### 2.2 原文不可变，理解可演进

- 用户投喂的原始内容一经落库，不允许被 AI 修改或覆盖。
- AI 的摘要、分类、关联和结论属于派生数据，可以产生新版本。
- 每一次演进都必须能回答：根据什么、何时发生、由谁产生、改了什么。

### 2.3 不确定就询问，不替用户编造上下文

- 对指代不明、冲突明显、影响较大的输入，系统必须进入待澄清状态。
- “第二个方案更好”“还是算了”等缺少对象的内容，不得被强行绑定到某个项目。
- AI 可以表达“不确定”，不得用流畅措辞掩盖证据不足。

### 2.4 模型判断与执行分离

- AI 输出新增、更新、合并、建立关联或忽略的结构化判断。
- 分类、标题、摘要等低风险操作由 Memory Engine 校验后自动提交，不要求用户逐条审批。
- 同一主题的可靠新证据可自动更新已有记忆，但必须创建新版本并继承全部来源。
- 删除、跨主题合并、覆盖用户确认过的结论、调用外部服务等高风险操作必须由用户确认。

### 2.5 模型可替换，记忆不可被模型绑定

- 数据使用公开、稳定、可迁移的格式存储。
- 业务状态不能只存在于模型上下文、向量数据库或某个供应商的私有结构中。
- 更换模型、Embedding 模型或推理框架，不得导致原始记忆丢失。

## 3. 不可逾越的边界

本节是强制约束。任何功能、模型提示词或后续架构调整都不得绕过这些规则。

### 3.1 数据主权红线

1. **不得扩大云端授权。** 用户已授权当前输入和必要候选记忆发送至 DeepSeek；附件、完整历史、数据库、索引、日志和导出文件仍不得上传。
2. **不得静默采集正文。** 常驻选区探测只允许读取 Windows UI Automation 暴露的“是否存在非空选区”和屏幕坐标，不得读取正文、剪贴板或键盘输入，不得扫描用户目录。
3. **不得扩大选区授权。** 点击投喂圆点后，只允许在本机内存暂存选中文字及同一 UI Automation 文本控件前后各最多 1000 字；不得跨控件、跨窗口、读取附件或来源应用文件。密码控件必须跳过。
4. **拖拽授权只覆盖拖入文字。** 微信等软件中的文字由用户主动拖到 FeedNote 图标后，只允许读取拖放载荷中的最多 4000 字纯文本；不得借此读取同一聊天的前后消息、剪贴板、拖放文件或图片。
5. **关闭选区弹窗必须无副作用。** 用户未选择“喂”“记”或“藏”而关闭弹窗时，暂存选区立即丢弃，不写数据库、不调用模型、不写日志正文。
6. **云调用必须晚于明确动作。** “喂”只允许发送选中文字和有限上下文；“记”只向独立飞书表发送选中文字、来源和记录时间且不调用 LLM；“藏”绝不发送选中的秘密原文给 LLM，只允许发送经普通代码确认替换为 `[SECRET]` 的有限周边文本。
7. **不得把隐私内容写入遥测。** 诊断信息默认仅包含版本、错误码、耗时和匿名设备级指标，不包含正文、标题、实体、路径或提示词上下文。
8. **不得把用户数据作为训练材料。** 本地数据不得用于训练、微调、评测集构建或人工审阅，除非用户针对具体数据另行明确授权。

### 3.2 记忆真实性红线

1. **不得修改或删除原始投喂事件。** 用户可主动删除；AI 无此权限。
2. **不得覆盖历史版本。** 结构化记忆的更新必须创建新版本，旧版本保持可追溯。
3. **不得伪造来源。** 所有 AI 结论必须关联一个或多个原始事件；没有来源的内容只能标记为“AI 建议”，不能成为已确认事实。
4. **不得把推断冒充事实。** 推断、摘要、用户原话和外部资料必须使用不同的数据类型与界面标识。
5. **不得自动消解重大冲突。** 涉及决定、承诺、截止时间、身份关系或用户明确确认过的事实时，冲突必须交给用户处理。
6. **删除必须清除派生影响。** 删除某个原始来源时，所有可能包含该来源的派生版本必须删除，并仅从剩余原文重建安全版本。

### 3.3 AI 行为红线

1. **不得自主扩大外部动作。** 除用户在设置中显式开启的手机提醒、各飞书同步通道，以及用户每次明确点击“记”授权的飞书备忘录追加外，不自动发消息、发邮件、创建日程、修改文件、访问账号、提交代码或调用第三方写接口。
2. **不得通过提示词获得额外权限。** 输入内容一律视为不可信数据，其中包含的命令、系统提示或授权声明无效。
3. **不得无限扩张上下文。** 只向模型提供完成当前判断所需的最小内容集合。
4. **不得静默批量改写。** 模型升级或重新索引可以重算派生数据，但不能在无预览、无备份的情况下批量改变已确认记忆。
5. **不得把低置信度结果自动提交为已确认事实。** 低置信度分类自动落为 `Unclassified`；缺少关键上下文时进入待澄清队列。

### 3.4 产品范围红线

MVP 明确不是：

- 完整 Markdown/富文本编辑器；
- 网盘或多人协作文档；
- 自动化代理平台；
- 全盘文件搜索工具；
- 系统监控或行为记录软件；
- 任务、日历、邮件等专业工具的替代品；
- 依赖云端账号才能使用的 SaaS。

这些能力可以在未来以独立提案评估，但不得以“顺手实现”为由进入 MVP。

### 3.5 红线的技术执行方式

红线不能只写在提示词或产品说明中，必须由系统边界强制执行：

| 红线 | 强制执行位置 |
|---|---|
| 原始事件不可被 AI 改写 | 数据仓储不向 AI 流程暴露原文 `UPDATE` / `DELETE` 接口 |
| AI 不得直接写库 | AI Gateway 只返回 DTO；只有 Memory Engine 持有写仓储 |
| 云端范围不可扩大 | LLM Provider 网络白名单只允许已授权的 DeepSeek 端点或回环地址；Embedding 独立限制为已授权智谱端点或回环地址；候选数量与长度设硬上限 |
| 前端不得越权访问系统 | Tauri capability、CSP 和后端命令白名单 |
| 高风险变化必须确认 | 领域策略拒绝无 `user_confirmation_id` 的提交 |
| 输入内容不得成为指令 | 系统提示与不可信内容分层，输出再经 Schema 和业务校验 |
| 删除必须由用户发起 | 删除命令要求交互产生的用户意图令牌，不接受模型生成的调用 |
| 选区正文不得静默读取 | UIA 常驻线程只调用选区范围与矩形接口；正文读取只由圆点点击命令触发 |
| 微信兼容不得变成被动采集 | 计划坞只处理 HTML5 文本拖放事件；后端限制 4000 字，不注册剪贴板监听、聊天窗口读取器或文件拖放读取路径 |
| 关闭选区弹窗不得产生副作用 | 内存快照清空；数据库写入只能位于“喂”“记”或“藏”命令之后，Provider 调用只能位于“喂”或脱敏后的“藏”链路 |
| 普通备忘录不得混入其他域 | 独立 `memo_records` 表；“记”不创建 `feed_events`、`memories`、`plans`、飞书任务、投递记录或秘密记录 |
| 编辑备忘不得丢失身份 | 只允许修改正文，保留本地备忘 ID、来源和首次记录时间；编辑后清空同步标记，飞书按本地 ID 覆盖原行，禁止追加重复行；同步完成标记必须校验正文仍是本次快照 |
| 飞书备忘录不得扩大上传 | 只单向写入本地备忘 ID、用户明确选择的原文、来源标题和记录时间；不上传周边上下文，不读取远端正文 |
| 秘密原文不得进入 LLM | “藏”先本地加密落库；独立脱敏函数精确替换选区，无法定位时丢弃周边正文；Provider 接口不接收秘密值字段 |
| 秘密不得混入普通数据 | 独立 `secret_records` 密文表与 Vault API；不创建 `feed_events`、`memories`、`plans` 或 FTS 行，常规导出器不查询该表 |
| 永久删除秘密必须重新认证 | 删除命令必须携带本次输入的主密码，并由后端重新执行 Argon2id 与校验密文验证；仅凭保险箱已解锁不得删除 |
| 飞书秘密同步不得反向覆盖 | 独立开关和独立表格 token；只回读本地秘密 ID 用于写后验证，不采用远端秘密字段，不扫描新行 |
| 周边读取不得扩张 | 仅移动同一 UIA TextRange 的首尾端点，前后各 1000 字，服务端再限制总长 6000 字 |
| 计划不得变成外部执行 | `plans` 先写本地 SQLite；手机提醒和飞书同步必须分别显式开启，只发送各自白名单字段；无日历、邮件、IM、文件写入接口 |
| 编辑计划不得改变身份或重复外部对象 | 主窗口只允许就地修改标题、时间、内容、详情、链接和注意事项；保留计划 ID、来源与创建时间，清除旧提醒并递增本地版本；飞书计划表按原 ID 覆盖原行，飞书任务按原 GUID 更新，禁止因编辑新建重复对象 |
| 启动项不得擅自创建 | 开机自启动默认关闭，只有用户在设置页明确开启并保存后才注册当前 FeedNote 可执行文件；关闭后移除启动项，读取时以操作系统实际状态为准 |
| 投递表写入不得越界 | 只读取配置目标的固定五列；新“公司＋岗位”组合可追加五列，匹配已有组合后只写该行 `状态` 单元格，禁止覆盖岗位、链接、备注、删除行、清表、改格式或访问其他工作表 |
| 投递记录不得因阶段变化重复 | 以“公司主体＋岗位方向”为联合主键，使用受控别名兼容常见公司全称/简称及岗位表述（如“前端”/“前端开发”/“前端工程师”）；禁止任意包含匹配，前端/后端等不同方向及同公司不同岗位必须分行；笔试/面试链接变化不得创建新行，多个匹配时只更新最早行 |
| 投递表不得反向生成计划 | 不注册后台扫描器，不把表格行交给计划生成器；计划只能来自本次明确投喂的选区 |
| 旧错误计划不得继续展示 | 升级迁移只按旧 `feishu_source_rows.plan_id` 精确移除派生计划，并排队清理计划表对应行；不得按标题猜测或修改投递记录表 |
| 投递记录与计划不得混用 | 路由结果分别通过投递表写入器和本地计划仓储执行；职位发布日期不得成为计划时间 |
| 飞书任务不得错分配 | 默认只使用用户配置的投递记录表所有者 `union_id`；不把应用自身当作负责人，不读取通讯录，不向其他用户分配任务 |
| 提醒时间不得漂移 | 飞书任务 `start` 与 `due` 均等于本地计划时间；唯一提醒取计划级 `reminder_minutes_before`，默认 180，用户明确要求准时则为 0；无完整时间不得创建任务 |
| 计划标签必须受控 | 当前只允许空标签或“面试”；模型未知标签必须丢弃，标签只参与 FeedNote 本地展示和筛选，不改变飞书表格列或任务内容 |
| 远端更新不得扩大权限 | 计划表只按已有本地计划 ID 回读完成状态；飞书任务只按已保存 GUID 回读标题、非全天开始时间和完成状态。不得从远端新建或删除计划，不回读任务描述、链接、内容、详情和注意事项；标题超过 80 字或时间无效时忽略对应字段 |
| 本地未同步变更不得被覆盖 | 本地版本晚于最近同步快照时跳过远端回读并推送本地状态；远端更新必须携带读取时的本地版本并通过原子条件更新 |
| 相对时间不得错误锚定 | 只有本次选区及其有限周边的时间可按捕获时间换算；证据不足时进入待澄清 |

红线测试属于发布阻断测试。任何模型、提示词或依赖升级后，只要越权测试失败，就不得发布。

## 4. MVP 范围

### 4.1 包含

- 桌面常驻应用和系统托盘入口；
- 普通备忘录一级导航、本地记录列表与独立飞书表单向追加；不接入 AI、不创建提醒；
- 全局快捷键唤起快速输入窗；
- 文本输入与手动粘贴；
- Windows UI Automation 选区圆点、“喂 / 记 / 藏”明确动作弹窗；
- 微信等自绘软件的显式文字拖放入口，复用“喂 / 记 / 藏”链路；
- 同一文本控件有限周边读取、AI 时间提取与缺失时间澄清；
- FeedNote 内部计划表、主窗口计划查看与编辑页面、透明桌面卡片和屏幕边缘折叠入口；
- 计划时间、标题、事项类型、可选链接和注意事项的结构化提取；
- 用户显式开启的单向手机提醒（ntfy 或通用 Webhook）；
- 用户显式开启的飞书电子表格固定字段写入与已有计划完成状态双向同步；
- 用户显式开启的飞书个人待办、已有任务标题/开始时间/完成状态双向同步与开始前 3 小时提醒；
- 用户显式开启的飞书投递记录写入、“公司＋岗位”去重和已有行仅更新状态；
- 独立主密码保护的本地加密秘密备忘录，以及显式开启的飞书秘密表单向明文同步；
- 投递记录与计划的独立自动路由，以及满足两类条件时的双写；
- 保存原始投喂记录；
- 自动识别记忆类型；
- 通过全文检索和语义检索寻找相关记忆；
- 提议 Create / Update / Link / Ask / Ignore；
- 低风险自动整理和高风险人工确认；
- 记忆详情、来源、版本时间线和撤销；
- 本地全文搜索、语义搜索和相关记忆召回；
- 数据导出、备份与彻底删除。

### 4.2 不包含

- 修改其他软件的右键菜单或注入其他进程；
- 后台读取剪贴板；
- 图片 OCR、音视频转写和复杂附件解析；
- 完整多设备数据同步、手机端编辑和多人协作；
- 自动调用邮件、日历、IM 或浏览器执行动作；
- 基于用户记忆自行发起持续运行的 Agent；
- 训练个人专属模型；
- 对整个知识库进行无人确认的自动重构。

选区入口当前仅支持 Windows UI Automation。未暴露可访问文本范围的软件不会显示圆点；MVP 不使用 OCR、剪贴板复制、进程注入或管理员权限绕过这一限制。

## 5. 信息类型与状态

### 5.1 记忆类型

```text
Memory
├── Knowledge   知识
├── Project     项目
├── Decision    决定
├── Idea        想法
├── Task        待办
├── Preference  偏好
├── Person      人物
└── Experience  经历
```

首版允许类型继续细化，但不得让类型体系阻塞记录。无法可靠分类时使用 `Unclassified`，不能为了分类完整性编造含义。

### 5.2 处理动作

```text
Feed
  ├── Create：形成新记忆
  ├── Update：为已有记忆创建新版本
  ├── Link：只建立关系，不改变双方内容
  ├── Ask：信息不足，等待用户澄清
  ├── Ignore：保留原始事件，不进入活跃记忆
  └── Review：存在冲突或风险，等待用户确认
```

`Ignore` 只表示不进入活跃记忆，绝不等于删除原始事件。

### 5.3 置信度不是权限

模型置信度仅供风险判断，不能单独决定是否允许操作。权限由“动作类型 + 数据影响 + 用户配置”共同决定。

建议默认策略：

| 操作 | 默认行为 |
|---|---|
| 创建普通知识、想法 | 自动提交，保留来源和撤销入口 |
| 建立弱关联 | 自动提交，可删除 |
| 更新未确认的普通摘要 | 自动创建新版本 |
| 修改决定、任务截止时间、人物关系 | 必须确认 |
| 合并两个独立主题 | 必须确认 |
| 删除原始事件或全部版本 | 只能由用户发起 |
| 调用外部服务 | 每类能力单独授权，MVP 禁止 |

## 6. 总体架构

建议技术栈：

- 桌面容器：Tauri 2；
- 前端：Vue 3 + TypeScript；
- 本地数据库：SQLite；
- 全文检索：SQLite FTS5；
- 模型接入：可替换 Provider，当前实现 DeepSeek Anthropic 兼容接口；
- 结构化输出：JSON Schema 校验；
- Embedding：独立的本地多语言向量模型；
- 密钥存储：操作系统 Keychain/Credential Manager；
- 数据迁移：显式 schema migration。

```text
┌──────────────────────────────────────────────┐
│                   UI Layer                   │
│ 快速输入 / 搜索 / 待澄清 / 记忆详情 / 设置   │
└──────────────────────┬───────────────────────┘
                       │ typed commands/events
┌──────────────────────▼───────────────────────┐
│              Application Layer               │
│ Feed Service / Review / Recall / Export       │
└───────────┬───────────────────────┬──────────┘
            │                       │
┌───────────▼──────────┐  ┌─────────▼──────────┐
│    Memory Engine     │  │     AI Gateway     │
│ 规则/检索/版本/关系   │  │ LLM/Embedding/校验 │
└───────────┬──────────┘  └─────────┬──────────┘
            │                       │ local by default
┌───────────▼───────────────────────▼──────────┐
│ SQLite + FTS5 + Vector Index + Blob Storage  │
└──────────────────────────────────────────────┘
```

### 6.1 模块职责

**UI Layer**

- 展示数据和收集用户意图；
- 不直接写数据库；
- 清楚区分原文、AI 自动推断、待澄清项和用户确认过的事实。

**Application Layer**

- 编排一次投喂的完整事务；
- 管理待澄清事项和用户授权；
- 对外提供稳定命令，不暴露模型实现细节。

**Memory Engine**

- 保存原始事件；
- 执行关键词检索、FTS 和向量召回；
- 应用自动提交策略；
- 创建版本、关系和审计记录；
- 保证不变量，拥有最终写入权。

**AI Gateway**

- 屏蔽 Ollama、云模型和具体模型差异；
- 约束输入上下文；
- 要求结构化输出并进行 Schema 校验；
- 返回候选动作，不直接写数据库。

AI Gateway 永远没有数据库写权限。模型输出只是提案，Memory Engine 才能根据规则提交。

## 7. 核心数据模型

### 7.1 双层存储

系统必须同时保存两类数据：

1. `FeedEvent`：用户实际投喂的原始事件，只追加，不原地修改；
2. `Memory` / `MemoryVersion`：系统对原始事件的当前理解，可演进、有版本。

建议的核心表：

```text
feed_events
  id, raw_content, content_type, source_type,
  source_metadata, created_at, deleted_at

memories
  id, memory_type, current_version_id,
  lifecycle_status, created_at, archived_at

memory_versions
  id, memory_id, title, body, structured_data,
  confidence, author_type, model_info,
  change_reason, created_at

memory_version_sources
  memory_version_id, feed_event_id, evidence_role

memory_relations
  id, from_memory_id, to_memory_id,
  relation_type, confidence, status, created_at

processing_runs
  id, feed_event_id, pipeline_version, model_info,
  input_hash, output_json, status, error_code, created_at

review_items
  id, proposed_action, payload_json, risk_level,
  reason, status, resolved_at

audit_log
  id, actor_type, action, target_type,
  target_id, metadata_json, created_at

vault_meta
  salt, verifier_nonce, verifier_ciphertext

secret_records
  id, nonce, ciphertext, created_at, updated_at, feishu_synced_at

memo_records
  id, content, source_title, created_at, feishu_synced_at

feishu_secret_cleanup_queue
  secret_id, queued_at
```

### 7.2 必须维持的数据不变量

- 每个 `MemoryVersion` 至少关联一个来源事件，纯 AI 建议除外；纯建议不能设为 `confirmed`。
- `memories.current_version_id` 必须指向该记忆自身存在的版本。
- 更新记忆只能新增 `MemoryVersion`，不能 `UPDATE` 旧版本正文。
- 自动化操作必须写入 `processing_runs` 和 `audit_log`。
- 软删除不清除审计链；彻底删除由用户主动触发，并清除正文、索引、向量、缓存和备份轮转中的对应数据。
- Embedding 是派生数据，可以重建，不能作为唯一事实来源。
- `secret_records` 的 `ciphertext` 必须包含秘密值及全部展示元数据；表内不得出现秘密标题、账号、网站或原文的明文副本。
- Vault 主密码不得持久化；内存中只保留 Argon2id 派生密钥，锁定或进程退出后清除。秘密不参与普通索引、候选召回和导出。

用户发现原文有误时，以“更正”事件追加新内容，并在事件之间建立 `corrects` 关系；旧原文不原地编辑。用户仍然拥有删除权，这与不可变原则不冲突：不可变约束防止系统偷偷改写历史，不限制数据所有者主动清除自己的数据。

永久删除后的审计记录只能保留不含正文、标题、实体和向量的最小操作凭证。应用管理的滚动备份必须在承诺的保留周期内同步清除；用户自行复制到应用控制范围之外的导出文件，应用无法代为删除，界面必须明确说明这一点。

## 8. 一次投喂的处理流程

```text
1. Capture
   立即保存 FeedEvent，先保证输入不丢失

2. Normalize
   普通代码处理时间、空白、URL 和显式元数据

3. Retrieve
   FTS + 向量检索召回少量候选记忆

4. Interpret
   LLM 输出类型、实体、候选动作、证据和不确定点

5. Validate
   JSON Schema + 业务不变量 + 风险策略校验

6. Decide
   自动提交低风险动作，其他动作创建 ReviewItem

7. Commit
   单个数据库事务内写版本、来源、关系和审计日志

8. Present
   告知用户“记成了什么”或提出一个具体澄清问题
```

任何一步失败，原始 `FeedEvent` 仍然存在，并标记为待重试。模型故障不能导致用户输入丢失。

## 9. AI 与普通代码的职责边界

### 9.1 应由普通代码完成

- 数据持久化、事务、版本管理和权限判断；
- 精确日期解析的确定性部分；
- FTS 检索、哈希去重和 ID 匹配；
- Schema 校验、字段长度限制和枚举检查；
- 文件访问、网络访问和外部服务调用；
- 数据导入导出、备份、删除和迁移；
- 自动提交与待澄清的最终裁决。

### 9.2 可由模型完成

- 记忆类型候选判断；
- 摘要、标题、实体和关键词提取；
- 新输入与候选记忆的语义关系判断；
- Create / Update / Link / Ask / Ignore 的候选提议；
- 冲突点说明；
- 生成简短、具体的澄清问题。

模型不得直接生成 SQL，不得获得文件系统工具，不得自行决定网络请求。

## 10. 检索与召回

首版采用混合检索：

```text
候选集 = FTS5 关键词结果 + Embedding 相似结果 + 最近活跃结果
最终排序 = 文本相关性 + 语义相似度 + 时间衰减 + 类型匹配
```

约束：

- Embedding 模型必须独立于生成模型，支持单独升级和重建；
- 召回给 LLM 的候选数量和总字符数必须有硬上限；
- 搜索结果必须能回到原始来源；
- 相关记忆只是建议，不表示因果关系或事实一致。

## 11. 本地模型与云模型策略

### 11.1 当前授权云模式

- LLM 默认使用 DeepSeek Anthropic 兼容端点和非思考模式的 `deepseek-v4-flash`；
- Key 只从 `data/secrets.env` 读取，不进入数据库、前端、日志或导出；
- DeepSeek Key 只发送到 DeepSeek；Embedding 使用独立智谱接口和独立/旧智谱 Key，禁止跨供应商复用 DeepSeek Key；无凭证或额度时回退本地 FTS5；
- 模型不可用时应用必须降级，而不是阻止原始记录落库。

### 11.2 未来本地模式

- 本地小模型必须实现同一 Provider DTO，不得绕过 Memory Engine；
- 本地与云端切换不得改变记忆表、版本表和来源关系；
- 本地 Embedding 需要独立索引版本，切换维度时必须可重建；
- 不允许在未显示实际 Provider 的情况下自动切换供应商。

## 12. 安全设计

### 12.1 威胁模型

首版重点防范：

- 投喂内容中的提示词注入；
- 模型输出越权写入；
- 云调用误传隐私；
- 日志、崩溃报告或缓存泄露正文；
- 数据库损坏和升级迁移失败；
- 本机其他普通用户直接读取数据文件。
- 模型请求、日志或普通导出意外泄露用户选择“藏”的秘密原文。

### 12.2 控制措施

- 模型输入明确分隔系统指令、候选记忆和不可信原文；
- 模型只返回受限 JSON，未知字段拒绝处理；
- Tauri 权限按最小能力配置，前端不能任意访问文件系统和 Shell；
- 所有写操作通过 Rust 后端命令和业务校验；
- 数据目录权限限制为当前操作系统用户；
- 敏感配置使用系统凭据存储；
- 默认日志脱敏，并提供完全关闭诊断日志的选项；
- 数据库迁移前自动生成可恢复备份；
- 导出文件由用户显式选择路径和范围。
- 秘密记录逐条使用 AES-256-GCM 和随机 nonce 加密，记录 ID 作为 AAD；主密钥通过 Argon2id 与随机盐从独立主密码派生。
- 飞书秘密表属于用户明确接受的明文副本，默认关闭、单向写入，并在设置页持续展示风险提示；飞书文档密码或分享权限不得描述为本地加密的替代品。

普通投喂、记忆和计划所在的数据库仍未整体静态加密，受系统账户与目录权限保护；只有 `secret_records` 使用应用层逐条加密。产品不得把局部加密描述成整个数据库“完全加密”。

## 13. 用户可控性

用户必须能够：

- 查看任何记忆的原始来源；
- 查看版本历史和每次变化原因；
- 撤销一次 AI 更新；
- 拒绝建议并避免同一建议反复出现；
- 将某条记忆标记为“仅保存，不参与 AI 上下文”；
- 暂停全部 AI 处理，继续作为普通本地记事本使用；
- 导出原始事件和结构化记忆；
- 删除单条、某个主题或全部数据；
- 查看本机模型和云模型的实际使用状态。

“撤销”通过切换当前版本实现，不能删除被撤销的历史版本。

## 14. 失败与降级策略

| 故障 | 系统行为 |
|---|---|
| LLM 未安装或未启动 | 保存原文，进入待处理队列，全文搜索仍可用 |
| LLM 输出格式错误 | 最多有限次数重试，之后进入待处理，不写错误结果 |
| Embedding 失败 | 保留 FTS 检索，异步补建向量 |
| 数据库写入失败 | 明确提示保存失败，不展示虚假的成功状态 |
| 应用处理中崩溃 | 根据 processing run 状态安全重试，避免重复版本 |
| 模型升级 | 保留旧结果；新结果预览或作为新派生版本写入 |
| 云服务不可用 | 回退本地模式，不自动切换到其他供应商 |

## 15. 性能与资源边界

MVP 目标：

- 点击保存后 200 ms 内完成原始事件落库并给出反馈；
- AI 整理异步执行，不阻塞窗口关闭或下一次记录；
- 常驻空闲内存目标小于 200 MB，不包含外部模型进程；
- 10 万条纯文本事件下，关键词搜索首屏目标小于 300 ms；
- 每次模型调用的候选记忆数量、字符数和超时时间均有配置硬上限；
- 后台任务必须可暂停，不得长期占满 CPU/GPU。

这些是工程目标，不是未经测量即可对外宣称的性能承诺。

## 16. 数据导出与迁移

至少支持：

- 原始事件导出为 JSONL；
- 记忆当前状态导出为 Markdown + JSON；
- 版本、来源和关系导出为结构化 JSON；
- SQLite 数据库完整备份；
- 导出格式包含 schema version。

导入必须先校验、预览数量和冲突，再写入新事务。导入文件中的文本同样是不可信数据，不能携带可执行指令。

## 17. 可观测性与审计

本地审计需要记录：

- 哪个输入触发了处理；
- 使用了哪个 pipeline、模型和提示词版本；
- 召回了哪些记忆 ID；
- 模型提出了什么动作；
- 规则为何自动接受或要求确认；
- 最终写入了哪些版本和关系。

默认不在普通日志中重复记录正文。调试模式如需展示正文，必须由用户临时开启并显示风险提示。

## 18. 工程目录建议

```text
src/                         Vue UI
src/components/              通用界面组件
src/features/feed/           快速投喂
src/features/memory/         记忆浏览与版本
src/features/review/         待澄清队列
src/features/search/         检索与召回
src-tauri/src/application/   用例编排
src-tauri/src/domain/        记忆模型与不变量
src-tauri/src/infra/db/      SQLite、迁移与仓储
src-tauri/src/infra/ai/      LLM/Embedding 适配器
src-tauri/src/infra/security/权限、密钥与脱敏
docs/                        设计决策与协议
```

领域层不得依赖具体模型供应商。AI 适配器不得绕过领域层直接写数据库。

## 19. 分阶段实施

### 阶段 0：无 AI 的可靠记录

- 快速输入、原始事件落库、列表和 FTS 搜索；
- 备份、导出、删除；
- 验证桌面常驻和快捷键体验。

### 阶段 1：自动分类，必要时询问

- 本地模型接入；
- 类型、标题和摘要自动写入新的记忆版本；
- 只有歧义、低证据冲突和高风险变化进入待澄清队列；
- 建立 processing run 和审计链。

### 阶段 2：受控自动演进

- 开放低风险 Create、Link 和普通摘要更新；
- 上线版本时间线、撤销和冲突处理；
- 用真实使用数据调整风险策略。

### 阶段 3：增强输入与可选云能力

- 评估系统分享菜单或右键扩展；
- 评估附件、OCR 和多设备同步；
- 评估显式授权的云模型增强。

每个阶段都必须独立可用，不允许为了未来能力提前突破当前权限边界。

## 20. MVP 验收标准

MVP 只有同时满足以下条件才算完成：

1. 断网时可记录、浏览、全文搜索和导出。
2. 模型宕机时输入不丢失，且能稍后重试。
3. 任意结构化记忆都能定位到来源和历史版本。
4. AI 无法修改原始事件，无法直接写数据库。
5. 模糊指代不会被静默绑定到现有项目。
6. 决定、截止时间等高风险变化会进入确认流程。
7. 默认配置不会向任何外部服务发送用户内容。
8. 用户可以暂停 AI，并继续使用基础记事功能。
9. 用户可以导出全部数据，并能彻底删除指定数据。
10. 数据库迁移失败时可以恢复，不损坏现有记忆。

## 21. 后续决策必须单独评审

以下事项不能直接进入开发，必须新增设计决策记录并重新评估隐私、权限和数据迁移：

- 系统级右键、剪贴板监听和其他被动采集；
- 云同步、多设备同步和多人协作；
- 自动执行任务、邮件、日历或第三方 API；
- OCR、录音、截图和屏幕内容理解；
- 长期自主运行的 Agent；
- 自动批量合并或重写历史记忆；
- 将数据用于模型训练或产品分析；
- 数据库加密方案及密钥恢复机制；
- 插件系统和第三方扩展权限。

评审至少要回答：用户是否明确知情、授权是否可撤销、最小数据范围是什么、失败能否恢复、历史能否追溯、功能是否会改变产品的信任模型。

## 22. 一句话架构准则

> 先可靠地保存用户说过什么，再谨慎地维护系统认为这意味着什么；前者永远不能被后者改写。
