# FeedNote v0.9 验收说明

## 直接启动

双击项目根目录的 `FeedNote.exe`。当前 Release 版本已在本机启动，数据库位于：

```text
D:\code\创业\桌面日记\data\feednote.db
```

## 建议验收流程

1. 在收集箱输入一段文字，按 `Ctrl + Enter`，确认记录立即出现在时间线。
2. 打开“记忆”，搜索刚才输入的关键词，确认能找到对应记忆。
3. 点击一条记录，查看当前理解、原始来源和版本时间线。
4. 运行 `./scripts/configure-deepseek.ps1` 写入 Key，在“设置”中测试连接，确认显示 `deepseek-v4-flash` 正常，并在 Embedding 无凭证或额度时提示回退本地全文检索。
5. 尝试永久删除，确认必须经过二次确认；取消时数据不变。
6. 在“设置”中导出 JSON，检查原始记录和当前记忆均在导出文件中。
7. 按 `Alt + Shift + Space`，确认 FeedNote 窗口被唤起并获得焦点。
8. 在浏览器、记事本或 Word 中选中文字，确认选区起点左上方出现粉色圆点；靠近屏幕边缘时圆点应保持完整可见，未点击圆点时不应弹窗。
9. 点击圆点后直接关闭弹窗，确认不新增记录和计划；再次选择并点“喂”，确认自动生成计划或询问具体时间。
10. 查看屏幕右侧 logo，点击展开透明计划卡片；标记完成后卡片从待办列表消失。
11. 从计划坞标题栏空白处拖动窗口，收起再展开，确认右边界和纵向位置保持不变；拖动标题下方透明度滑杆，确认背景透明度实时变化、文字仍清晰，重启后数值保留。
12. 投喂含 `https://` 链接的面试通知，确认卡片只显示时间、标题、内容、链接和注意事项，点击链接由系统浏览器打开。
13. 在 `data\secrets.env` 配置推送地址，在“设置 -> 手机提醒”中开启并发送测试提醒，确认手机收到消息。
14. 在飞书应用后台发布应用并开通表格读、写、创建权限，将凭证写入 `data\secrets.env`；在“设置 -> 飞书计划表”开启并初始化，确认生成“FeedNote 计划”表格。
15. 新增一条计划后检查表格新增一行；补充时间或标记完成后，确认原行更新而不是产生重复行。
16. 打开设置页，确认不再显示“飞书投递记录”开关、链接、连接检查或同步入口；旧数据库中已开启的投递同步会自动变为关闭，旧链接不被删除。
17. 投喂一段包含公司、岗位和招聘链接但没有未来行动的职位信息，确认只进入记忆库，桌面不生成计划，投递表没有新增或更新。
18. 模拟模型返回 `writeApplicationRecord=true` 和完整投递字段，确认本地解析会强制改为 `false`、清空投递字段，提交命令也不会调用飞书写表。
19. 投喂一段与求职无关、包含明确时间的待办，确认只进入 FeedNote 本地计划及计划表，不写投递记录表。
20. 投喂包含公司、岗位、面试类型和明确时间的面试邀请，确认生成带“面试”标签的计划卡片并同步计划表，但不新增或更新投递记录。
21. 投喂仅包含网页发布日期或职位发布日期的招聘页，确认该日期不会被识别为计划时间，投递表保持不变。
22. 重复职位、笔试、面试和拒绝通知投喂，确认 FeedNote 对投递表均不产生单元格读取或写入；表格现有内容保持不变。
23. 从曾启用“投递表分析”的旧数据库升级，确认只有带旧来源映射的派生计划被移除，真实选区计划保留；FeedNote 计划表对应旧行被清除，投递记录表保持不变。
24. 开通 `task:task:write` 和 `sheets:spreadsheet.meta:read`，开启“飞书待办提醒”并立即同步；确认每条有时间的未完成计划仅创建一个个人飞书任务。
25. 检查飞书任务负责人为投递记录表所有者，`start` 与 `due` 等于计划时间；未指定提醒时唯一提醒为提前 180 分钟，明确写“准时提醒/不用提前”时为 0 分钟；待补充时间的计划不创建任务。
26. 修改计划时间或标题后同步，确认原任务更新而非重复创建；标记计划完成后飞书任务完成，重新打开后同一任务恢复为待办。
27. 在飞书任务中修改已映射任务的标题、非全天开始时间、完成或重新打开，保持 FeedNote 运行并等待不超过 30 秒，确认主窗口计划页、桌面卡片和计划表对应行同步变化；开始与截止不同的时候，本地计划采用开始时间。修改任务描述、全天时间或新建陌生飞书任务不得覆盖或创建本地计划。
28. 在“FeedNote 计划”表格中只修改已有行的“状态”为“已完成”或“已安排”，等待不超过 30 秒，确认本地状态变化；修改标题、时间或新增行不得影响本地计划。
29. 本地刚修改完成状态但尚未同步时，将飞书保留为旧状态，确认本地变化优先且最终覆盖远端旧状态。
30. 选中一段没有确定时间的产品想法，点击浮球后选择“记”；确认弹窗快速关闭，侧边栏“备忘录”出现原文、来源和记录时间，桌面计划、投递记录、普通记忆和秘密备忘录均不新增内容。
31. 投喂会让模型生成较长计划描述的内容，确认计划摘要被自动压缩到 60 字以内，不再因摘要过长导致整次投喂失败。
32. 对两条待安排计划依次回答“明天上午，时间你来安排”，确认第二条会避开第一条并保持至少 60 分钟的开始间隔；明确回答“明天上午九点半”时不得擅自改时。
33. 选中一个测试用 API Key，点击圆点后选择“藏”；首次使用设置至少 6 个字符的独立主密码，确认浮窗立即成功并可在短时间内撤销。
34. 打开“秘密备忘录”，解锁后确认可查看名称、类型、账号、网站和备注；显示、复制和隐藏均可用。点击编辑，修改名称、类型、账号、秘密值、网站或备注，确认保存后重新加密且仍为同一条记录。
35. 检查普通收集箱、记忆、桌面计划、全文搜索和 JSON 导出，确认测试秘密没有进入这些区域；模型服务日志中不得出现选中的秘密原文。
36. 退出并重启应用，确认必须重新输入主密码；错误密码不能解锁，应用不提供主密码找回或重置现有保险箱的入口。
37. 在秘密备忘录已解锁时开启“设置 -> 飞书秘密表”并初始化，确认创建独立“FeedNote 秘密”表格；秘密值按风险提示以明文显示，后续新增和修改只从本地单向更新飞书。
38. 在飞书端修改秘密值或新增行，确认不会反向覆盖或创建本地秘密；删除本地秘密后确认对应远端行被清空。
39. 点击秘密卡片的删除按钮，确认必须重新输入主密码；错误密码不得删除或关闭弹窗，正确密码才永久删除。保险箱仅处于已解锁状态不能绕过这一步。
40. 在微信中选中文字并长按拖动，将其拖到屏幕右侧折叠的 FeedNote 图标；图标应显示粉色投放状态，松开后出现“喂 / 记 / 藏”弹窗，三个入口分别进入原有投喂、普通备忘和秘密保险箱链路。
41. 向 FeedNote 图标拖入图片或文件，确认不会读取、打开、投喂或显示处理弹窗；对文字拖拽产生的弹窗直接关闭后，确认不新增记录且不调用模型。
42. 左键单击系统托盘图标，确认打开主窗口且不弹出菜单；右键系统托盘图标或桌面浮标，确认出现“打开 FeedNote / 退出 FeedNote”原生菜单，点击退出后主窗口、浮标和后台进程全部终止。
43. 设置页默认显示“手动启动”；开启“开机自启动”并保存后，确认 Windows 当前用户启动项包含指向项目根目录 `FeedNote.exe` 的记录；再次打开设置仍显示开启。关闭并保存后确认启动项被移除，应用当前进程不退出。
44. 第一次点击“记”后等待飞书同步，确认自动创建独立“FeedNote 备忘录”表格，且只包含本地备忘 ID、原文、来源和记录时间；断网时本地记录仍成功，恢复网络后可在备忘录页面手动同步。
45. 点击备忘卡片右上角编辑按钮，修改正文并保存；确认本地卡片立即显示新正文，来源和首次记录时间不变，状态变为等待同步。同步完成后确认飞书按本地备忘 ID 更新原行而非新增行；空正文和超过 4000 字的正文不得保存。
46. 打开主窗口左侧“桌面计划”，确认可切换“进行中 / 全部”，计划卡片显示时间、标题、内容、详情、链接和注意事项；编辑后计划 ID、来源、创建时间及飞书任务 GUID 不变，飞书计划表更新原行且飞书待办更新原任务。修改时间后旧提醒记录必须清除；清空时间后计划进入待安排状态。标记完成与重新打开应同步到桌面计划坞和飞书。
47. 投喂一条面试计划，确认自动显示“面试”标签；主窗口和桌面计划坞初始均显示全部标签，切换为“面试”后只展示带该标签的计划。编辑器可在“无标签 / 面试”之间修改，模型返回其他标签时不得落库；升级前已有的明确面试计划应一次性自动补标。

## 本轮完成

- Tauri 2 + Vue 3 + TypeScript 桌面应用；
- SQLite、WAL、FTS5 和显式数据库迁移；
- 原始事件与结构化记忆双层模型；
- 原始记录、记忆版本、来源、处理运行、审核项和审计日志；
- 快速输入、时间线、全文搜索、类型筛选和记忆详情；
- DeepSeek Anthropic Provider、`deepseek-v4-flash` 非思考模式、结构化输出校验和安全降级；
- 分类、标题和摘要自动落库，并保留原始版本；
- 自动判断新建、更新已有记忆或建立记忆关联；
- 更新已有记忆时继承全部来源，删除来源时从剩余原文安全重建；
- 只有歧义内容进入待澄清区；
- 两阶段永久删除，后端要求一次性确认令牌；
- JSON 数据导出；
- 系统托盘和全局快捷键；
- 项目图标、响应式界面和浏览器预览适配层；
- D 盘本地工具链、依赖缓存、构建缓存和应用数据目录。
- UI Automation 全局选区几何探测和粉色投喂圆点；
- “喂 / 记 / 藏”选区弹窗、有限周边读取和缺失时间追问；
- 本地计划表、透明桌面计划卡片和屏幕边缘折叠入口。
- 结构化计划字段、受控外链打开和可拖动计划坞；
- 默认关闭的 ntfy / Webhook 手机提醒与防重复调度。
- 默认关闭的飞书电子表格同步、固定字段白名单和失败重试。
- 永久停用的飞书投递记录读写：旧设置自动关闭、设置入口移除、模型输出清零、提交写入分支和旧写入实现均已移除。
- 职位信息进入记忆、明确未来事项进入计划，且不存在投递表反向生成计划的后台扫描器。
- 飞书计划表只双向同步已有计划的完成状态；飞书待办对已有映射任务双向同步标题、非全天开始时间和完成状态，本地未同步变更优先。
- AES-256-GCM 本地秘密保险箱、Argon2id 独立主密码和解锁后的秘密备忘录界面。
- “藏”链路的原文隔离：秘密不进入 LLM、普通记忆、计划、全文索引或常规 JSON 导出；模型只接收可验证脱敏的周边文本。
- 默认关闭的飞书秘密表明文单向同步，独立于计划表和投递记录表。
- 微信等自绘软件的文字拖放入口，只接收纯文本并复用“喂 / 记 / 藏”处理链路。
- 普通备忘录本地列表与独立飞书表单向追加；“记”不调用模型、不创建计划或提醒。
- 主窗口桌面计划页面，支持进行中/全部视图、结构化编辑、完成与重新打开，并复用原飞书行和飞书任务映射。

## 自动验证结果

```text
前端测试：11 passed
Rust 领域测试：51 passed，3 个在线测试默认 ignored
DeepSeek 在线 Provider 测试：需配置 `DEEPSEEK_API_KEY` 后单独运行（结构化分类、同主题自动更新、投递写入阻断与精确计划时间）
TypeScript 检查：passed
Vite production build：passed
Tauri release build：passed
Release 启动：FeedNote 进程与 WebView 正常响应
```

说明：本轮尝试自动执行真实 Windows 选区点击截图验收时，Computer Use 辅助组件因本机 kernel assets 路径缺失无法启动。因此选区圆点、授权弹窗和计划坞仍需按上方第 8-10 步做一次人工视觉验收；文档不把“进程存活”冒充为已完成的点击验收。

Rust 测试覆盖：原始来源不可变、自动分类、已有记忆更新、来源删除重建、关闭澄清不改写记忆、全文搜索、选区点位、计划澄清状态迁移、Provider 地址白名单和密钥解析。

前端测试覆盖：记录与来源一致、自动分类、永久删除级联、无确认令牌拒绝删除、未授权模型地址拒绝和统计更新。

## 明确未包含

- 未安装本地 LLM；未来通过同一 Provider 边界接入小模型。
- 未修改系统或其他软件的右键菜单；当前入口是独立的选区圆点。
- 未实现剪贴板监听、OCR、目录扫描或对不支持 UI Automation 的软件进行强制读取。
- 智谱 Embedding 保持独立可选配置；无凭证或额度时运行时自动使用 SQLite 全文搜索，DeepSeek Key 不会发送到智谱。
- 未实现完整多设备数据同步、手机端编辑和附件 OCR；当前手机能力是单向计划提醒。
- 未实现整个数据库的静态加密；普通投喂、记忆和计划仍依赖本机账户与目录权限，秘密记录已单独加密。
- 未实现导入和自动备份轮转；本轮只提供显式 JSON 导出。

这些能力仍受 [TECHNICAL_DESIGN.md](TECHNICAL_DESIGN.md) 中的边界约束，不能在没有单独评审时直接扩展。

## 开发命令

```powershell
.\scripts\dev.ps1
.\scripts\test.ps1
.\scripts\build.ps1
```

脚本统一把 Rust、Cargo、npm、构建产物和运行数据指向 D 盘项目目录。
